new constraints ::
	every location has ::
		flags
		a root (optional)
		an index (optional)
	sub-folders can, if not explicitly stated, refer to the folders they're a part of (for index/flags/exec)
	extensions can carry flags or execution value
	configuration files dont *need* a timeout, index, exec info or error files

solutions ::
	instead of a single index -> map <location, index>
	for locations roots -> map <location, alter>
	sub-folders checks (already half implemented) -> i send a list instead of a single string, iterate on the list (+ previous, for details see the code in ServerConfig::isMethodAllowed)
	for extensions (done) -> added check when checking methods/executability
	non-mandatory info -> adjust functions like isExecFolder() or getIndex() for an appropriate return on no info




first steps ::
	----- 	see below :: maybe at this point i just send the list prepped, iteration loop for the checks
	retroactive flags check -->tested, should work well
		implement that same retroactive method for the index search once index is a locations mapped list
		and the same for the cgi folder check (hard-coded for now)
	exec and allowed methods apply to extensions also
	alter egos :: hard-code for now, but the logic is in
	now to wait for the end of the 100,000,000 bytes testing
