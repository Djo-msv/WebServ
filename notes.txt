cgi env variables still possibly missing ::
	SERVER_SOFTWARE - name/version of HTTP server (??)
	REMOTE_HOST — host name of the client, unset if server did not perform such lookup.
	REMOTE_ADDR — IP address of the client (dot-decimal).
	REMOTE_IDENT — see ident, only if server performed such lookup.

supposedly, we want ::  "http://$SERVER_NAME:$SERVER_PORT$SCRIPT_NAME$PATH_INFO" to point to be a gettable url

question :: is it possible to put down a decimal number in the timeout variable ? next update we'll try that
	answer is yes !! we can set it to .05 if we feel like rushing the fcking job
	but it's not recommended (example POST 0, method is checked too fast "0" is left over on the read channel)

-----------------------------------------------------------

02/04, adding static POST requests

changed seekFile to have a bool post for request changes
response needs a ->ifPost() from request

compilation went fine now retesting
	testing for post success x
	testing for no file x
	testing for folder x
	testing for index x
	
	testing regular server behavior x


-----------------------------------------------------------

clean up notes :: 01/04
	main.cpp :: 
	if (waitRequest.timeout()) { status = Done; } --> no Bad Request return ? (ex the request has no sep)
		possible solution :: { parse(); } //with a argument-dependent parse (MissingData becomes BadRequest throws)
	to avoid valgrind leaks, maybe we change the signal for stopping the server ? (SIGQUIT or sthg maybe)
	Request.cpp ::
	SERVER_SOFTWARE cgi env variable might need renaming
	getting rid of the PATH env var in cgi
	added LengthRequired error
	might want to put the target_work *after* the body parsing :: get full request first, then check if its server-compatible
	Response.cpp ::
	deleted the "unknown" content-type :: no known type, no header

	next up :: Socket/ + socket_parsing.cpp

clean up notes :: 02/04
	ClientSocket.cpp ::
	errorHandling exec is always false, no more exec bool
	left a)creating socket b)resetting socket messages
	ServerConfig.cpp ::
	maybe a few explanatory comments might help
	unsure if we need the ::isExecFolder() last two conditions in last if (just last should do) -> fixed
	ServerSocket.cpp ::
	listen 5 on queue seems very small to me for some reason
	Socket.cpp ignored;
	socket_utils.cpp ::
	pretty self-explanatory, but comments are never wasted
	socket_parsing.cpp ::
	same

clean up notes :: 05/04
	ServerConfig.cpp ::
	added comment of locations delete in the destructor since we have leaks
	however, adding these delete creates invalid reads since the destructor is apparently called before
	proposed solutions ::
		pointers everywhere (starting from the initServerConfig return)
		deep copy constructor(s)

